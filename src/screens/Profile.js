import React from 'react';
import { StyleSheet, Text, Image, View } from 'react-native';


export const Profile = ({iconColor, icon , text}) => {
  return (
    <View style={styles.IWD}>
        
    </View>
  );
};

const styles = StyleSheet.create({
    
  });
